# BondCat — Tauri 桌宠

- src: 当前目录 `/root/bondcat/`，GitHub `shuitian23-bot/bondcat`
- 本地 Mac **无 clone**
- 版本: `package.json` + `src-tauri/tauri.conf.json` 一起改
- **Linux 服务器不能 build DMG**，改完 push → 本地 Mac pull 跑 `cargo tauri build`
- 改完必写 `ITER_LOG.md`

详情 @README.md @ITER_LOG.md
